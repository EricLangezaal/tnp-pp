generators:
  train:
    _target_: icicl.data.kolmogorov.KolmogorovGenerator
    data_dir: /Users/matt/projects/tnp-pp/data/kolmogorov
    re_dirs:
      50: re-50
      100: re-100
      200: re-200
      500: re-500
      1000: re-1000
      2000: re-2000
      5000: re-5000
      10000: re-10000
    split: train
    batch_grid_size: [16, 16, 16]
    min_num_ctx: 1
    max_num_ctx: 500
    min_num_trg: 1000
    max_num_trg: 1000
    samples_per_epoch: 16000
    batch_size: 16
  val:
    _target_: icicl.data.kolmogorov.KolmogorovGenerator
    data_dir: /Users/matt/projects/tnp-pp/data/kolmogorov
    re_dirs:
      50: re-50
      100: re-100
      200: re-200
      500: re-500
      1000: re-1000
      2000: re-2000
      5000: re-5000
      10000: re-10000
    split: valid
    batch_grid_size: [16, 16, 16]
    min_num_ctx: 1
    max_num_ctx: 500
    min_num_trg: 1000
    max_num_trg: 1000
    samples_per_epoch: 4096
    batch_size: 16

params:
  dim_x: 3
  dim_y: 2

misc:
  project: kolmogorov
